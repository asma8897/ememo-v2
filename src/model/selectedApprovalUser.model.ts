import { User } from "../model/user.model";
import { Group } from "./group.model";
// import { MemoAttachment } from "./memo-attachment.model";

export interface SelectedApproval {
    groupData: Group;
    // userData: User;
    // groupId: number;
    // memoData: MemoAttachment;
    // memoId: number;
    // memoStatusId: number;
    // createdOn: Date;
}